import { StyleSheet } from 'react-native';
import { theme } from '../../global/styles/theme';

export const styles = StyleSheet.create({
    container: {
        height: 2,
        width: '85%',
        backgroundColor: 'black' /*theme.color.line*/,
        display: 'flex',
        top: 10
    }
});